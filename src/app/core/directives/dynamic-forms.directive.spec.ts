import { DynamicFormsDirective } from './dynamic-forms.directive';

describe('DynamicFormsDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicFormsDirective();
    expect(directive).toBeTruthy();
  });
});
